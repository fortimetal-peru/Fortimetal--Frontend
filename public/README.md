Esta carpeta está vacía a propósito — Vite la sirve tal cual en la raíz del sitio
(por ejemplo, `public/favicon.ico` quedaría accesible en `/favicon.ico`).

Solo existe este archivo para que Git pueda rastrear la carpeta (no rastrea carpetas
vacías). Si tu explorador de archivos te muestra un ".DS_Store" u otro archivo oculto
al momento de subir, no viene del proyecto — lo agrega tu sistema operativo al
descomprimir; puedes ignorarlo o borrarlo sin problema.
